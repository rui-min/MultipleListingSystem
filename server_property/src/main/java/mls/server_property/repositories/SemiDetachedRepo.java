package mls.server_property.repositories;

import mls.server_property.domain.SemiDetached;
import org.springframework.stereotype.Repository;

@Repository
public interface SemiDetachedRepo extends FreeholdRepo<SemiDetached> {
}